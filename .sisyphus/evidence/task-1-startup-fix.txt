=== QA Evidence: task-1-startup-fix ===
Date: 2026-05-27
File: src/core/remote/remote_http_server.cpp

=== Scenario: 正常起動 — Start() が true を返す ===
Steps:
  1. grep "bind_to_port" remote_http_server.cpp → Found: "if (!server->bind_to_port(bind_address, port)) {"
  2. grep "listen_after_bind" remote_http_server.cpp → Found: "server->listen_after_bind();"
  3. grep -c "server->listen(" remote_http_server.cpp → 0 (old pattern removed)
  4. grep -c "start_promise" remote_http_server.cpp → 0 (promise removed)
Result: PASS - New pattern in place, old deadlock pattern completely removed

=== Scenario: ポート使用中 — bind失敗時に即座に false を返す ===
Steps:
  1. Check bind_to_port return value → Found: "if (!server->bind_to_port(bind_address, port)) { ... return false; }"
Result: PASS - bind failure returns false immediately, does not proceed to listen_after_bind

=== Additional checks ===
  - grep -c "promise" remote_http_server.cpp → 0
  - grep -c "future" remote_http_server.cpp → 0
  - #include <future> removed from file
  - Stop() unchanged
  - IsValidBindAddress() unchanged
  - pre_routing_handler unchanged
  - Bounded 5s wait with 10ms polling used (not indefinite wait_until_ready())

=== Summary ===
All verifications PASS. The Start() function now uses a two-phase
bind-then-listen approach that avoids the blocking deadlock of server->listen().
