Scenario: m_remote_server_enabled完全除去
  Steps:
    1. grep "m_remote_server_enabled" src/core/core.h → 0件
    2. grep "m_remote_server_enabled" src/core/core.cpp → 0件
    3. Read core.cpp ~807 → nullptrベース条件確認
  Expected: フラグ完全除去、nullptrチェックに置換
  Result: PASS (grep 0件、nullptr比輀ロジック確認)

Scenario: port変更検知
  Steps:
    1. grep "m_remote_server_port" src/core/core.cpp → 比較ロジック存在
    2. grep "m_remote_server_bind_address" src/core/core.cpp → 比較ロジック存在
  Expected: port/bind変更が検知される
  Result: PASS (core.cpp:816-821 で比較、reset+needs_creation トリガー)

Scenario: ApplySettings遅延化
  Steps:
    1. Read core.cpp ~149-153 → ApplySettings()呼出しが除去されている
  Expected: ApplySettings()がシグナル処理から除去
  Result: PASS (ApplySettings() 呼び出し除去、frame_limit.SetValue のみ残存)

Scenario: Start()戻り値チェック
  Steps:
    1. grep "Start()" src/core/core.cpp → 戻り値チェックあり
  Expected: Start()失敗がLOG_ERROR出力される
  Result: PASS (core.cpp:586, 833 で if (!remote_server->Start()) チェック、LOG_ERROR(Remote_Services, ...) 出力)

Scenario: IsPoweredOnガード
  Steps:
    1. Read emulator_handler.cpp stop/reset → IsPoweredOn()チェックあり
  Expected: stop/resetが非起動時に400を返す
  Result: PASS (stop: line 26-27, reset: line 33-34 で IsPoweredOn() ガード追加)
